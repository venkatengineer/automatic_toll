version: '3.8'

services:
  backend:
    build: .
    ports:
      - "7000:7000"
    volumes:
      - .:/app
    environment:
      - SECRET_KEY=supersecretkey
      - DATABASE_PATH=/app/instance/locations.db
      - FLASK_DEBUG=1
    restart: always
